server.port=8080
spring.application.name=webhook

# Shared secret used by Ticketmaster to sign the webhook and by the user-app to verify it.
# For demos only.
webhook.sharedSecret=dev-secret

# Reject callbacks older than this (basic replay protection)
webhook.maxClockSkewSeconds=300
