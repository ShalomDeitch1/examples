waitingroom:
  sqs:
    queue-name: waiting-room-joins
    region: us-east-1
    access-key: test
    secret-key: test
    endpoint: "" # for LocalStack, set to e.g. http://localhost:4566
    auto-create-queue: true
  processing:
    batch-size: 10
    rate-ms: 200

server:
  port: 8080

logging:
  level:
    software.amazon.awssdk: ERROR
