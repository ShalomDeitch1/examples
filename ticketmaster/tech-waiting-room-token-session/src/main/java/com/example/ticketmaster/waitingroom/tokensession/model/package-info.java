/**
 * Domain model for the token/session waiting room.
 *
 * <p>The model is intentionally minimal: callers care about a session ID and its status.
 */
package com.example.ticketmaster.waitingroom.tokensession.model;
