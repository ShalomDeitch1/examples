server.port=8080
spring.application.name=short-url-simplest

# Enable actuator shutdown endpoint for testing crash logic
management.endpoint.shutdown.enabled=true
management.endpoints.web.exposure.include=health,shutdown
